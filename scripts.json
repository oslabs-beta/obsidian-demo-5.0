{
  "$schema": "https://deno.land/x/denon@2.5.0/schema.json",
  "scripts": {
    "start": {
      "cmd": "deno run --allow-net --allow-read --allow-env --unstable --no-check server.tsx",
      "desc": "run my app.ts file",
      "watch": true
    }
  }
}